import { component$ } from "@builder.io/qwik";

export default component$(() => {
  return (
    <footer class="rounded-b-lg ">
      <div class="">Made with OpenAi GPT and News API </div>
    </footer>
  );
});
