import { component$ } from "@builder.io/qwik";

export default component$(() => {
  return (
    <footer class=" rounded-b-lg">
      <div class="p-5">Made with OpenAi GPT and News API </div>
    </footer>
  );
});
