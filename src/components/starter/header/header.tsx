import { component$ } from "@builder.io/qwik";

export default component$(() => {
  return (
    <header class="rounded-t-lg text-center">
      <div class="p-5">
        {/* <h2 class="text-white text-3xl font-bold">Quiz App</h2> */}
      </div>
    </header>
  );
});
